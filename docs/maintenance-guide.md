# Maintenance Guide
Run `python scripts/update_information.py` to check approved sources. Run `python scripts/automated_debugger.py` to validate repository health. Review all automated results before presenting information as authoritative.
